name: Check
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install deps
        run: cd Web-Cypress && npm install
      - name: Lint
        run: cd Web-Cypress && npm run lint
      - name: Prettier
        run: cd Web-Cypress && npm run prettier
